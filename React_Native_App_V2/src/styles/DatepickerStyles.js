import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    inputStyle: {
        textAlign: 'center',
        padding: 10,
        margin: 1,
        fontWeight: 'bold',
        fontSize: 16
    }
});

export default styles;